import os
import sys
import pandas as pd

# ==== Configure Paths ====
SOURCE_FILE = r"C:\path\to\your\source.csv"
OUTPUT_DIR = r"C:\path\to\your\output\folder"
LOOKUP_FILE = r"C:\Users\Sourav.Sindhikar\Downloads\Mapping\Case_alliance_accountLKP.xlsx"

# ==== Constants ====
ARROW_ACCOUNT_ID = "001Wr00000mKja2IAC"
UNITY_ACCOUNT_ID = "001Wr00000mKWszIAG"
DEFAULT_BH_ID = "01mWr0000012RrmIAE"

def ensure_lookup(path):
    if not os.path.exists(path):
        print(f"❌ Lookup file not found: {path}")
        sys.exit(1)
    return path

def load_lookup(path):
    print("📖 Loading lookup file...")
    df = pd.read_excel(path, dtype=str).fillna("")
    for col in df.columns:
        df[col] = df[col].astype(str).str.strip()

    required = {"Legacy_SF_Record_ID__c", "Id"}
    missing = required - set(df.columns)
    if missing:
        print(f"❌ Lookup file missing columns: {', '.join(missing)}")
        sys.exit(1)

    mapping = {
        legacy.lower(): new_id
        for legacy, new_id in zip(df["Legacy_SF_Record_ID__c"], df["Id"])
        if legacy.strip()
    }

    return mapping

def apply_account_logic(row, acct_lookup):
    rt_name = str(row.get("Account.Recordtype.Name", "")).strip().lower()
    current = str(row.get("AccountId", "")).strip()
    key = current.lower()

    if "arrow / verical" in rt_name:
        return ARROW_ACCOUNT_ID
    if "unity" in rt_name:
        return UNITY_ACCOUNT_ID
    if key and key in acct_lookup:
        return acct_lookup[key]
    return current

def main():
    print("=== Case Account & Business Hours Updater ===")

    if not os.path.exists(SOURCE_FILE):
        print(f"❌ Source file not found: {SOURCE_FILE}")
        sys.exit(1)

    os.makedirs(OUTPUT_DIR, exist_ok=True)
    main_output = os.path.join(OUTPUT_DIR, "cases_updated.csv")

    lookup_path = ensure_lookup(LOOKUP_FILE)
    acct_lookup = load_lookup(lookup_path)

    print("🔄 Reading source CSV...")
    df = pd.read_csv(SOURCE_FILE, dtype=str).fillna("")

    print("🧭 Updating AccountId / BusinessHoursId...")
    df["AccountId"] = df.apply(lambda row: apply_account_logic(row, acct_lookup), axis=1)
    if "BusinessHoursId" in df.columns:
        df["BusinessHoursId"] = DEFAULT_BH_ID

    print("⚙️ Mapping Expedite_Supplier__c...")
    if "Expedite_Supplier__c" not in df.columns:
        df["Expedite_Supplier__c"] = ""

    def map_expedite(row):
        legacy = str(row.get("Expedite_Supplier__c", "")).strip().lower()
        if legacy and legacy in acct_lookup:
            return acct_lookup[legacy]
        return row.get("Expedite_Supplier__c", "")

    df["Expedite_Supplier__c"] = df.apply(map_expedite, axis=1)

    print(f"💾 Saving output: {main_output}")
    df.to_csv(main_output, index=False, encoding="utf-8-sig")
    print("🎉 Done!")

if __name__ == "__main__":
    main()
